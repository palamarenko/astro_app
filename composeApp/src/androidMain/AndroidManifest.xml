<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Уведомления: обязательно для Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Рекламный идентификатор: обязательно для Android 13+ при использовании AdMob -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Iruna"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Black.NoTitleBar"
        android:networkSecurityConfig="@xml/network_security_config"
        android:extractNativeLibs="false">

        <!--
            TODO: Замените тестовый AdMob App ID на ваш реальный из AdMob консоли:
            https://apps.admob.com → Настройки приложения → App ID
        -->
        <!-- 16 KB page size compatibility for Android 15+ -->
        <property
            android:name="android.pageSizeCompat"
            android:value="true" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Firebase Cloud Messaging: получение data-сообщений -->
        <service
            android:name=".notifications.PushNotificationService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
